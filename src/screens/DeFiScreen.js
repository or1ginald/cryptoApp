import {StyleSheet, Text, View} from 'react-native';
import React from 'react';

const DeFiScreen = () => {
  return (
    <View>
      <Text>DeFiScreen</Text>
    </View>
  );
};

export default DeFiScreen;

const styles = StyleSheet.create({});
