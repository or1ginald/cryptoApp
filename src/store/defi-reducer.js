const initialState = {
  currency: [
    {name: 'fffff'},
    {name: 'fffff'},
    {name: 'fffff'},
    {name: 'fffff'},
  ],
};

export const defiReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
